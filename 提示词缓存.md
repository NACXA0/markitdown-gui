1-在设置里增加设置：
1-2-radio：拖入文件立即转换（默认）、点击“转换”按钮开始、单个文件立即转换，只有选择多个文件才展示文件列表
1-2-switch明暗设置（默认明亮）
1-3-switch悬浮窗（悬浮球的形式悬浮，拖动文件到悬浮球立即转换）（默认关闭）
1-4-switch为输出文件增加时间辍前缀
1-5-switch MCP开关（默认关闭），可以给本机的其他AI直接使用此转换工具，获取转换文件甚至直接返回文本内容（分析哪种方式更好）

2-单个文件的转换是否不应该持久保存？
3-另外，对于传入的同名文档，输出要自动重命名，以及提醒。
4-也许需要增加反向的转换？
5-需要.AppImage（x86），并且开发时构建这一种。为ARM（所有类型）、rpm、exe、app（macos）打包格式留出占位